package src;

/**
 * Liste des enums utilisées !
 *
 * @author Nicolas
 *
 */

// Celle-ci c'est pour les voisinages.
enum Direction {
	HG, HD, D, G, BG, BD;
}

enum NiveauDensite {
	un, deux;
}

// Force du vent
enum Force {
	faible, moyen, fort;
}
